# .env.example — 複製為 .env 後填入真實金鑰
# 本機啟動：cp .env.example .env && vim .env

# Gemini API 金鑰（必填，從 https://aistudio.google.com/apikey 取得）
GEMINI_API_KEY=your-gemini-api-key

# 資料目錄（音檔 + SQLite，相對於 backend/ 或絕對路徑）
DATA_DIR=data

# CORS 允許來源（逗號分隔，開發時預設 localhost:3000）
CORS_ORIGINS=http://localhost:3000
