# 專案 Prompt Engineering 指引

> **用途**：定義專案特定的 AI 行為指引，讓 agent 輸出更貼合領域需求
> **所有區塊皆為可選**：未填寫的區塊，agent 使用內建預設值
> **填寫時機**：`adf.design` 初始化專案時選擇 preset，或任何時候手動編輯
> **大小建議**：每個 agent 範例建議 50-100 行以內，避免佔用過多 context window
>
> **快速開始**：從 `.project/context/presets/` 複製一個適合的 preset 覆蓋此檔案：
> ```bash
> cp .project/context/presets/embedded.md .project/context/prompt-guide.md
> ```

---

## 🧠 角色補充

> 此區塊補充領域特定的思考方式，影響所有 agent 的決策傾向。
> **留空時**：agent 使用內建的通用角色定義。

[在此描述你的領域背景、關注重點、決策傾向。例如：]
[- 我們的產品是嵌入式視訊設備，記憶體和 CPU 是稀缺資源]
[- 設計時優先考慮最壞情況（WCET）而非平均效能]
[- 所有動態分配都需要有上限，偏好 static allocation]

---

## 📏 評估框架

> `/planner`（architect）比較方案時使用的評估維度和評分標準。
> **留空時**：architect 使用內建通用 5 維度框架（效能、可維護性、可擴展性、團隊可行性、風險，各 20%）。
> **覆寫規則**：此處定義的維度**取代**內建預設（不是疊加）。

### 評估維度

| 維度 | 評估重點 | 1 分（差） | 5 分（優） |
|------|----------|-----------|-----------|
| [維度1] | [重點] | [差的標準] | [優的標準] |
| [維度2] | [重點] | [差的標準] | [優的標準] |
| [維度3] | [重點] | [差的標準] | [優的標準] |
| [維度4] | [重點] | [差的標準] | [優的標準] |
| [維度5] | [重點] | [差的標準] | [優的標準] |

### 權重規則

[根據專案階段或產品類型的權重分配，例如：]
[- Prototype 階段：維度1 30%、維度2 20%、...]
[- Production 階段：維度1 20%、維度2 30%、...]

---

## ⚠️ 風險標準

> `/planner`、`/breakdown`、`/develop` 判斷變更風險等級的標準。
> **留空時**：使用通用風險標準（新增檔案=LOW、修改邏輯=MEDIUM、改介面=HIGH）。
> **覆寫規則**：此處定義的標準**取代**內建預設。

| 風險等級 | 判斷標準 | 額外要求 |
|----------|----------|----------|
| 🟢 LOW | [標準] | [要求] |
| 🟡 MEDIUM | [標準] | [要求] |
| 🔴 HIGH | [標準] | [要求] |

---

## 🔍 自我審查

> 各 agent 在輸出前的自我檢查問題。
> **留空時**：使用各 agent 內建的通用審查問題。
> **覆寫規則**：此處定義的問題**疊加**到內建預設之上（不取代）。

### 架構設計（architect）

1. [領域特定的審查問題]
2. [領域特定的審查問題]

### 開發實施（developer）

1. [領域特定的審查問題]
2. [領域特定的審查問題]

### 代碼審查（code-reviewer）

1. [領域特定的審查問題]
2. [領域特定的審查問題]

---

## 📝 範例

> 各 agent 的 few-shot example，展示期望的輸出深度和格式。
> **留空時**：agent 使用 zero-shot 模式（無範例，依靠 prompt 指引）。
> **啟用規則**：有定義時自動啟用 few-shot 模式。
> **大小建議**：每個範例 50-100 行以內。

### architect 範例

[一個完整的方案設計範例，包含：]
[- 需求分析（硬約束/軟約束）]
[- 2 個方案比較]
[- 結構化評估表格（使用上方定義的維度）]
[- 推薦方案和條件說明]
[- 實施計劃（含風險標記）]

### developer 範例

[一個開發決策的範例，展示：]
[- 架構邊界的判斷]
[- 風險標記的使用]
[- self-critique 的執行]

### code-reviewer 範例

[一個審查報告的範例，展示：]
[- 嚴重度分級（Critical/Major/Minor/Suggestion）]
[- 具體的改進建議]

---
*此檔案由 `adf.design` 時的 preset 選擇或手動填寫產生*
*Preset 範例：`.project/context/presets/`*
